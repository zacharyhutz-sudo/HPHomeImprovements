# Assets

The site recreates the public Wix design with public image URLs referenced from `src/data/siteData.js`.

A few Wix URLs respond as AVIF through the browser even when the displayed filename ends in .jpg/.png, so they were left as public references instead of being renamed into misleading local files. Vendor marks are recreated as simple text/logo cards to avoid bundling trademarked files.
